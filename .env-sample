# Rename this file to .env

API_TOKEN= Account > Settings > Generate New Token
CANVAS_BASE_URL=https://canvas.nus.edu.sg
OUTPUT_PATH= Local machine output folder path

# Comma-separated course codes to sync, or "all" to sync every active course. Empty/missing = sync all.
COURSES=CS3223,CS3234